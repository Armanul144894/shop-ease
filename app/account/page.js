import AccountPageView from '../../components/storefront/AccountPageView';

export const metadata = {
  title: 'Account | ShopEase Studio',
  description: 'Customer account, saved details, and order history.',
};

export default function AccountPage() {
  return <AccountPageView />;
}
